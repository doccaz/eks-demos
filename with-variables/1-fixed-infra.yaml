# fixed-infra.yaml
# Version-agnostic shared templates. Machine templates are now in the versioned bundles.
apiVersion: infrastructure.cluster.x-k8s.io/v1beta2
kind: AWSManagedClusterTemplate
metadata:
  name: cluster-fixed
  namespace: default
spec:
  template:
    spec: {}
---
apiVersion: bootstrap.cluster.x-k8s.io/v1beta2
kind: EKSConfigTemplate
metadata:
  name: bootstrap-fixed
  namespace: default
spec:
  template:
    spec: {}
